# Set this to your Cloud Run service URL after deploying (no trailing slash, no path)
# Example: VITE_ADK_URL=https://immerse-ai-xxxx-uc.a.run.app
# For local dev leave blank — falls back to http://localhost:8000
VITE_ADK_URL=
